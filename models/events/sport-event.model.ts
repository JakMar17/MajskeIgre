export type SportEventModel = {
    title: string;
    date: Date;
    location: string;
    price?: string;
    teamFormation?: string;
    registerLink?: string;
    description?: string;
    imageUrl?: string;
}